check if topologicalSort works correctly
check why can't write formula that depends on other column
handle column deletion when having dependending columns
implement saving and opening project file
restrict formula columns only to numerical columns